import React from 'react';

interface AdminProps {}

const Admin = (props: AdminProps) => {
	return (
		<div className="mx-auto mt-5 w-25">
			<div className="alert alert-info text-center">Admin Page</div>
		</div>
	);
};

export default Admin;
