import React from 'react';

interface ComposeProps {}

const Compose = (props: ComposeProps) => {
	return (
		<div className="mx-auto mt-5 w-25">
			<div className="alert alert-info text-center">Compose Page</div>
		</div>
	);
};

export default Compose;
